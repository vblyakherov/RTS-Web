{
  "name": "rtks-tracker-frontend-tests",
  "version": "1.0.0",
  "description": "Frontend unit tests for RTKS Tracker (utils.js / api.js logic)",
  "private": true,
  "scripts": {
    "test": "jest --no-coverage",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/*.test.js"],
    "setupFiles": ["./jest.setup.js"]
  }
}
