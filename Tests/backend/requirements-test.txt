# Core testing
pytest==8.3.2
pytest-asyncio==0.23.8
anyio[trio]==4.6.2

# Async HTTP client for FastAPI testing
httpx==0.27.2

# Async SQLite for in-memory test DB (avoids needing a running PostgreSQL)
aiosqlite==0.20.0

# All production dependencies (installed from backend/requirements.txt)
# Run: pip install -r ../../backend/requirements.txt -r requirements-test.txt
